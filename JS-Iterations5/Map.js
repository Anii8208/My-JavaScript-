//Map Method, loop, function = it is also called call back function it dosent require True condition it work on any condtion

const Mynum = [1, 2, 3, 4, 5, 6, 7, 8, 9]

//const MyNewNum = Mynum.map((num) => num+10 )

//console.log(MyNewNum);

//*********************************************************************************** */

//Chaining in map = 2 or more method at same time, First result is store in second method

//const MyNewNum = Mynum.map((num) => num*10).map((n) => n+1).filter((num) => num>=31)

//console.log(MyNewNum);